// This is just a dummy file to suppress an error.
// For whatever reason, the Pyodide JS code attempts to
// load a nonexistent `stackframe.js` file from the root directory.
// This results in an annoying create-react-app error overlay
// when in development mode.
// To suppress this, I created this dummy file,
// so that the request to load `stackframe.js` will succeed
// (albeit do nothing).
